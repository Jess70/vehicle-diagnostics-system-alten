# Backend Configuration
NODE_ENV=production
PORT=3000
DATABASE_PATH=/usr/src/app/data/vehicle_diagnostics.db

# Redis Configuration  
REDIS_HOST=localhost
REDIS_PORT=6379

# MinIO Configuration
MINIO_ENDPOINT=localhost:9000
MINIO_PUBLIC_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin123
MINIO_BUCKET=vehicle-logs

# File Upload Constraints
SUPPORTED_FORMATS=.txt,.log  # Used by storage service validation

# Log Processing Configuration
LOG_PARSE_BATCH_SIZE=100  # Number of log entries to process per batch

# MinIO Root Credentials (for MinIO service itself)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
